The three tools read the grid, so they work for any domain. For a new region, repeat
Steps 1–5 pointing at that region's `croco_grd.nc` and folders, then run the driver with
`--rivers`. After the one-time setup, every forecast cycle stages the river file
automatically.